package pack;

public class PackageMain1 {
    public static void main(String[] args) {
        Data data = new Data();
        pack.a.User user = new pack.a.User();
        //다른 패키지에 클래스가 존재할 경우 경로를 적어서 불러오면 된다.
    }
}
